Freestyle {
    agent any

    stages {
        stage('Cleanup Workspace') {
            steps {
                cleanWs()
            }
        }
        
        stage('Checkout') {
            steps {
                git url: 'https://github.com/lololis/VulnerableJavaWebApplication.git'
            }
        }
        
        stage('Fortify Local Arguments') {
        steps {
            fortifyLocalArguments transOptions: '-Xmx4G',
            scanOptions: '"-analyzers" "controlflow,dataflow"'
            }
        }
        stage('Fortify Local Analysis') {
        steps {
                fortifyLocalAnalysis localAnalysisProjectType: fortifyPHP(),
                uploadSSC: [appName: 'SAST_25', appVersion: 'v25']
                }
            }
        stage('Build') {
            steps {
                echo "Complete!"
            }
        }
    }
}
